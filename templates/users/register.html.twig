{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}
{% block body %}
<div class="login-page">
    <div class="form">
        <div id="welcome">Užsiregistravote !!!</div>
        {{ form(form, {'attr': {'class': 'register-form', 'id': 'user_register_form','novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        <p class="message">Already registered? <a href="{{ url('login') }}">Sign In</a></p>
    </div>
</div>
{% endblock %}
